npx lint-staged --allow-empty

# Skip code-simplifier if SKIP_SIMPLIFIER is set
if [ -n "$SKIP_SIMPLIFIER" ]; then
    exit 0
fi

# Get staged files (excluding deleted files)
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx|py|go|rs|sh)$' || true)

if [ -n "$STAGED_FILES" ]; then
    echo "🔧 Running code-simplifier on staged files..."

    # Create a comma-separated list for the prompt
    FILE_LIST=$(echo "$STAGED_FILES" | tr '\n' ',' | sed 's/,$//')

    # Run code-simplifier via Claude CLI in non-interactive mode
    if claude -p "/code-simplifier Review and simplify these staged files: $FILE_LIST" --dangerously-skip-permissions 2>/dev/null; then
        # Re-stage any changes made by the simplifier
        echo "$STAGED_FILES" | while read -r file; do
            if [ -f "$file" ]; then
                git add "$file"
            fi
        done
        echo "✅ Code simplification complete"
    else
        echo "⚠️  Code-simplifier skipped (Claude CLI not available or failed)"
    fi
fi
